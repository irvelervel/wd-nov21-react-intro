import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))
// ReactDOM is the react engine working in the browser
// render() is a function capable of outputting the first component of the application
// it's mounting our first component (App) into the DOM

// this is the file that starts everything :)
